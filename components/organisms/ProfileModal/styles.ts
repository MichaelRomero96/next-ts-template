import { makeStyles } from '../../../theme';

const useStyles = makeStyles()(() => ({
  root: {
    fontSize: '20px',
    padding: 20,
  },
  userProfile: {
    textAlign: 'center',
    paddingBottom: 20,
  },
}));

export default useStyles;
